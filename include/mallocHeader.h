#ifndef MALLOCHEADER_H
#define MALLOCHEADER_H

#define MIN_MEMORY 10                     // Minimum number of integers memory is allocated for, can be increased as needed

void average(int *arr, int length);       // Averages the numbers in an array

void mallocIntegers();                    // Dynamically allocates memory for an array of integers based on user input

#endif